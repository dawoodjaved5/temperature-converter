version: "3.9"

services:
  app:
    build: .
    container_name: temperature-converter
    ports:
      - "3000:3000"
    volumes:
      - converter_logs:/app/logs
    environment:
      - LOG_DIR=/app/logs
    restart: unless-stopped

volumes:
  converter_logs:

